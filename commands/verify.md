# Verify RAG capability

Run the project-owned RAG evaluation lane through the independent verifier. Require phase evidence,
zero authorization leakage, valid citations and abstention, quality thresholds, provider-failure
behavior, latency/cost budgets, and client/integration behavior before accepting delivery.

