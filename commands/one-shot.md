# Build RAG capability

RAG participates automatically in `$start-build` when deterministic PRD detection marks it required.
Use the shared phase order: architecture during requirements, client slices, backend ingestion and
retrieval, typed integration, independent evaluation, then delivery. Do not run this file as a shell
command and do not create a standalone RAG application outside the selected monorepo.

