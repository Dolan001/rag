# Before RAG verification

Release implementer leases and use the independent verifier. Require a versioned evaluation set,
production-shaped permissions, exact-search recall baseline where ANN is used, and explicit PRD
thresholds. Verification cannot mutate application code or lower thresholds.

