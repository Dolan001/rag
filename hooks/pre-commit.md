# Before committing RAG work

Require valid phase evidence and zero authorization leakage. Scan staged content for provider secrets,
private source text, uploaded documents, model traces, and sensitive evaluation fixtures. Reject
unrelated files and generated vector/index data.

